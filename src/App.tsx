import { Input } from './components/Input/Input';
import './index.css';

function App() {
  return (
    <div>
      <Input loading={false} onChange={() => {}} />
    </div>
  );
}

export default App;
